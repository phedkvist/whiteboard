import History from "./History";

describe("History", () => {
  it("Initiate class with empty values", () => {
    // const history = new History();
    // expect(history.changes.length).toEqual(0);
  });
});

export {};
